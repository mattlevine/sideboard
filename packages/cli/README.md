# `@sideboard/cli`

Agent-agnostic worktree orchestration from the terminal.

```bash
npm i -g @sideboard/cli

sideboard ls
sideboard send <thread> "…"
sideboard mcp          # MCP stdio server
```

Also installs `side` as a short alias. The MCP server is available as `sideboard mcp`, or via `@sideboard/core`'s `sideboard-mcp` bin.
