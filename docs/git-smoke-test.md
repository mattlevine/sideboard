# Git smoke test

Disposable note used to exercise the Sideboard worktree git loop
(commit, push to `origin`, draft PR).

## Runs

- #11 — first loop, then retest on `origin/main` after #10 (HTTPS fallback).
- This change — rerun after #11 merged; base `2a006c4`.

Safe to close without merging.
