# Git smoke test

Disposable note used to exercise the Sideboard worktree git loop
(reset onto `origin/main`, commit, push to `origin`, draft PR).
Retest after #10 (HTTPS push fallback when ssh-agent is missing).
Safe to close without merging.
